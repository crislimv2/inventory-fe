import { DashboardCanvas } from "@/components/dashboard/DashboardCanvas";

export const metadata = { title: "Dashboard — Toko Ci Ali" };

export default function DashboardPage() {
  return <DashboardCanvas />;
}
